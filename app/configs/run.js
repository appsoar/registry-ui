'use strict';

angular.module('registryUiApp').run(function($rootScope, $location){
    // $rootScope.$on('$stateChangeSuccess', function(newV) {
    //         $rootScope.path = $location.path().split('/')[1];
    //         alert($rootScope.path);
    // });
});